# Eval

The eval suite contains:

- 80 intent classification cases.
- 18 retrieval cases.
- 14 campus QA cases.

Metrics are computed from predictions and agent outputs, then written to JSON and Markdown reports. The runner records prompt version, model version, case counts, and metrics. Scores are not hardcoded.

