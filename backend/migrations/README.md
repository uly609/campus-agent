# Migrations

The demo migration command uses SQLAlchemy metadata in `backend/app/domain/models.py` to create tables for local validation. Alembic configuration is present so production migration scripts can be added without changing application code.

